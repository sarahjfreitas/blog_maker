= form_with(model: [@post, @post.comments.build], local: true ) do |form|
  %p
    = form.label :name
    = form.text_field :name
  %br
  %p
    = form.label :body
    = form.text_area :body
  %p
    = form.submit